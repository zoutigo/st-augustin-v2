import { PageHolder } from "@/components/page-holder";

import React from "react";

type Props = {};

const InfosPage = async (props: Props) => {
  return (
    <PageHolder>
      <div> Les informations</div>
    </PageHolder>
  );
};

export default InfosPage;
