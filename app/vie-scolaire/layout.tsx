export const dynamic = "force-dynamic";
export const revalidate = 0;

const VieScolaireLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default VieScolaireLayout;
