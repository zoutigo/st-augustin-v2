import { PageHolder } from '@/components/page-holder';
import React from 'react';

type Props = {};

const EditPage = (props: Props) => {
  return <PageHolder>Edit Page</PageHolder>;
};

export default EditPage;
